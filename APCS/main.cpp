#include <bits/stdc++.h>
using namespace std;

void solve() {
  int a, b;
  cin >> a >> b;
  vector<int> v1(a), v2(b);
  for (auto &i : v1) cin >> i;
  for (auto &i : v2) cin >> i;
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  solve();
}